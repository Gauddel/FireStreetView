/*'use strict';

angular.module('jujuvu2App').factory('User', ['$firebaseObject',
  function($firebaseObject) {

   /* var User = $firebaseObject.$extend({

      getFullName: function() {
        return this.firstName + " sssss" + this.lastName;
      }
    });
    return function(userId) {
      var ref = new Firebase("https://jujuvu.firebaseio.com/users/").child(userId);
      return new User(ref);
    }
  }
]);*/